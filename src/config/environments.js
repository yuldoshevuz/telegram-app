require('dotenv').config()
const {
    API_ID,
    API_HASH,
    STRING_SESSION,
    NIKNAME,
    TG_USERNAME,
    DELETE,
} = process.env

module.exports = {
    API_ID,
    API_HASH,
    STRING_SESSION,
    NIKNAME,
    TG_USERNAME,
    DELETE,
}